{
  "name": "markup-set",
  "version": "1.0.0",
  "description": "markup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "markup:scss": "node-sass -w -r markup/scss -o public/css --output-style expanded",
    "build:scss": "node-sass markup/scss -o public/css --output-style compressed",
    "markup": "npm run markup:scss",
    "build": "npm run build:scss"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "~2.4.1",
    "express": "^4.16.3",
    "node-sass": "^4.9.2"
  }
}
